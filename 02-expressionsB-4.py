name = input('What is your name? ')
print('Nice to meet you', name)
#                       ^ this is important . use a comma to separate a string and a variable call

# function asks the user the user for input( here ) and then stores that input as a variable